import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tarea_taller',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
